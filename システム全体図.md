---
author: claude
type: wiki
created: 2026-04-16
updated: 2026-05-28
tags: [hpb, salonboard, system, flowchart]
source: ""
---

# HPB予約システム 全体図

> **2026-05-28 更新**: Vercel Blob → Cloudflare R2 移行完了 / ローカルPC → ConoHa VPS（pm2常時起動）移行完了 / GitHub Actions 廃止

---

## 全体アーキテクチャ

```mermaid
flowchart TB
    subgraph CLIENT["👤 お客様"]
        C1["自社予約フォーム\nreservation.html"]
        C2["HPB（ホットペッパー）"]
    end

    subgraph VERCEL["☁️ Vercel（ホスティング）\n Hobby Plan / 12 Serverless Functions"]
        direction TB
        API1["api/salonboard.js\n空き枠API\n(/api/slots もここへルーティング)"]
        API2["api/reserve.js\n予約受付"]
        API3["api/reservations.js\n予約一覧"]
        API4["api/admin-action.js\nキャンセル・変更"]
        API5["api/checkin.js\nチェックイン"]
        API6["api/checkout.js\n会計・メール"]
        API7["api/sales.js\n売上集計"]
        API8["api/customers.js\n顧客カルテ"]
        API9["api/customer-note.js\nカルテメモ"]
        API10["api/receipt.js\n領収書"]
        API11["api/cron.js\nリマインダー/朝レポート"]
        API12["api/comingsoon.js\nカミングスーン"]
        HTML["index.html / admin.html\nreport.html / customers.html\nreservation.html 等"]
    end

    subgraph R2["☁️ Cloudflare R2（ストレージ）"]
        R2A[("slots-data.json\n空き枠データ")]
        R2B[("reservations-queue.json\nキュー")]
        R2C[("salonboard-YYYY-MM.json\n月別予約")]
        R2D[("today-reservations.json\n本日の予約")]
    end

    subgraph VPS["🖥️ ConoHa VPS（常時起動 / pm2）"]
        W["worker.js --watch\n60秒ポーリング"]
        SS["sync_slots.js\n1時間ごと自動"]
        SE["sync_from_email.js\nHPBメール解析"]
        PW["Playwright / Firefox\n（headless）"]
    end

    SB["📋 SalonBoard\n（美容室管理システム）"]
    HPB["📧 HPB通知メール\n（Gmail）"]
    MAIL["📧 Resend\nメール送信"]

    C1 -->|予約フォーム送信| API2
    API2 --> R2B
    API1 --> R2A
    API1 --> R2B

    W -->|キュー読み取り| R2B
    W --> PW
    PW -->|自動登録・キャンセル| SB
    W -->|1時間ごと| SS
    SS --> PW
    PW -->|スクレイピング| SB
    SS -->|書き込み| R2A
    SS -->|書き込み| R2C
    SS -->|書き込み| R2D

    C2 -->|予約| HPB
    HPB -->|メール解析| SE
    SE -->|キュー更新| R2B

    API2 --> MAIL
    API6 --> MAIL

    style VERCEL fill:#e8f4ff,stroke:#0066cc
    style R2 fill:#f0e8ff,stroke:#6600cc
    style VPS fill:#e8ffe8,stroke:#006600
    style SB fill:#fff8e0,stroke:#cc8800
    style CLIENT fill:#fff0e8,stroke:#cc4400
```

---

## 1. 予約受付フロー（自社フォーム）

```mermaid
flowchart TD
    A["👤 お客様\nreservation.html"] -->|① サービス選択| B["GET /api/slots?service=hair\n→ api/salonboard.js"]

    subgraph salonboard_api["api/salonboard.js — 空き枠計算"]
        B --> C1[("☁️ R2: slots-data.json\n（SalonBoard空き枠）")]
        B --> C2[("☁️ R2: reservations-queue.json\n（自社フォーム予約済み）")]
        C1 --> C3["キュー分を差し引いて\n空き枠を返す"]
        C2 --> C3
    end

    C3 -->|② 空き枠カレンダー表示| A
    A -->|③ 日時・氏名など入力して送信| D["POST /api/reserve.js"]

    subgraph reserve_api["api/reserve.js — 二重チェック"]
        D --> E1["① slots-data.json チェック\n電話・直接予約で埋まっていないか"]
        E1 -->|埋まっている| ERR1["409 この時間帯は埋まっています"]
        E1 -->|空き| E2["② reservations-queue.json チェック\n自社フォーム予約が満席でないか"]
        E2 -->|満席| ERR2["409 この時間帯は満席です"]
        E2 -->|空き| E3["予約をキューに追加\nR2へ保存"]
    end

    E3 --> M1["📧 確認メール（Resend）"]
    E3 --> M2["📱 スタッフ通知\nメール / LINE"]

    style A fill:#fff8f0,stroke:#e60
    style C1 fill:#f0e8ff,stroke:#66c
    style C2 fill:#f0e8ff,stroke:#66c
    style ERR1 fill:#ffe0e0,stroke:#c00
    style ERR2 fill:#ffe0e0,stroke:#c00
    style E3 fill:#e8ffe8,stroke:#080
```

---

## 2. SalonBoard 登録フロー（worker.js — ConoHa VPS）

```mermaid
flowchart TD
    W["🖥️ worker.js --watch\nConoHa VPS / pm2 常時起動"]

    W -->|60秒ごと| Q["processQueue()\nR2: reservations-queue.json"]
    Q -->|pending があれば| PROC["status → processing"]
    PROC --> P["Playwright Firefox\nSalonBoard に自動登録/キャンセル"]
    P -->|成功| S1["status: completed"]
    P -->|失敗| S2["status: failed"]

    W -->|1時間ごと| SS["syncSlotsIfNeeded()\n↓\nnode sync_slots.js"]

    subgraph sync_slots["sync_slots.js — SalonBoard スクレイピング"]
        SS --> L1["Firefox起動\n.state/playwright.lock で排他制御"]
        L1 --> L2["SP版ログイン\nhttps://salonboard.com/login_sp/"]
        L2 --> L3["PC版トップへ遷移\n/CLP/bt/top/ (60秒・失敗しても続行)"]
        L3 --> L4["スケジュールページ遷移\n/CLP/bt/schedule/ (120秒)"]
        L4 --> L5["HTML解析\nempty_time_sid_fix_* パターン"]
        L5 --> L6["空き枠・予約データ生成"]
    end

    L6 --> BL1[("☁️ R2: slots-data.json\n空き枠 + 月別予約")]
    L6 --> BL2[("☁️ R2: salonboard-YYYY-MM.json\n月別予約一覧")]
    L6 --> BL3[("☁️ R2: today-reservations.json\n本日の予約")]

    Note["⚠️ キャンセル3日以内は\n即座にsync_slots実行"]
    S1 --> Note

    style W fill:#e0f4e0,stroke:#080
    style SS fill:#e0f4e0,stroke:#080
    style BL1 fill:#f0e8ff,stroke:#66c
    style BL2 fill:#f0e8ff,stroke:#66c
    style BL3 fill:#f0e8ff,stroke:#66c
    style Note fill:#fef3cd,stroke:#c90
```

---

## 3. HPB経由フロー

```mermaid
flowchart TD
    H1["👤 お客様\nHPBで予約"] --> H2["HPB → SalonBoard\n自動反映（HPB側の仕組み）"]
    H1 --> H3["📧 HPB通知メール\n（新規・キャンセル・変更）\nGmailに届く"]
    H3 --> H4["sync_from_email.js\n（VPS or ローカルで手動実行）\nメール解析・種別判定"]
    H4 --> BQ[("☁️ R2: reservations-queue.json\n追記")]
    BQ --> W1["worker.js\nSalonBoard登録・キャンセル"]

    style H1 fill:#fff8f0,stroke:#e60
    style BQ fill:#f0e8ff,stroke:#66c
```

---

## ファイル対応表

| ファイル | 役割 | 状態 |
|---|---|---|
| `reservation.html` | 自社予約フォーム（Vercel ホスト） | ✅ 完成 |
| `admin.html` | スタッフ用予約管理画面（パスワード保護） | ✅ 完成 |
| `register.html` | スタッフ登録・顧客カルテ管理 | ✅ 完成 |
| `report.html` | 売上レポート（支払い別・日別・リピート率） | ✅ 完成 |
| `customers.html` | 顧客カルテ一覧 | ✅ 完成 |
| `api/reserve.js` | 予約受付（R2二重チェック＋確認メール＋スタッフ通知） | ✅ |
| `api/salonboard.js` | 空き枠API（`/api/slots` もvercel.jsonでここへルーティング） | ✅ |
| `api/reservations.js` | 予約一覧取得（管理画面用） | ✅ |
| `api/admin-action.js` | キャンセル・変更処理（管理画面用） | ✅ |
| `api/checkin.js` | チェックイン処理 | ✅ |
| `api/checkout.js` | 会計完了・サンクスメール | ✅ |
| `api/sales.js` | 売上集計API | ✅ |
| `api/customers.js` | 顧客カルテ API | ✅ |
| `api/customer-note.js` | カルテメモAPI | ✅ |
| `api/receipt.js` | 領収書API | ✅ |
| `api/cron.js` | Vercel Cron（前日リマインダー / 朝レポート） | ✅ |
| `api/comingsoon.js` | カミングスーン管理 | ✅ |
| `sync_slots.js` | SalonBoard空き枠スクレイピング → R2保存（Firefox/Playwright） | ✅ |
| `sync_from_email.js` | HPBメール解析 → R2キュー更新 | ✅ |
| `sync_comingsoon.js` | カミングスーン同期 | ✅ |
| `worker.js` | 予約SalonBoard登録・1時間ごとsync（VPS pm2常時起動） | ✅ |

> **Vercel Hobby Plan 上限**: Serverless Functions 12本。`api/slots.js` は削除済み（`vercel.json` で `/api/slots → /api/salonboard` にルーティング）。

---

## R2 データ構造（Cloudflare R2）

| ファイル名 | 内容 |
|---|---|
| `reservations-queue.json` | 自社フォーム予約キュー（status: pending / processing / completed / failed） |
| `slots-data.json` | SalonBoard空き枠（`{ slots, slotCounts, serviceSlots, activeStylists, slotsByDateRaw }`） |
| `salonboard-YYYY-MM.json` | 月別予約一覧（管理画面の日別予約参照用） |
| `today-reservations.json` | 本日の予約一覧（管理画面トップ用） |

---

## インフラ構成

| コンポーネント | 役割 | 備考 |
|---|---|---|
| **Vercel** | フロントエンド + Serverless API | Hobby Plan / 12関数上限 |
| **Cloudflare R2** | ストレージ（全JSONデータ） | 旧: Vercel Blob |
| **ConoHa VPS** | worker.js 常時起動 + sync_slots.js | pm2で管理 |
| **SalonBoard** | 美容室管理システム | Playwright/Firefoxで自動操作 |
| **Resend** | メール送信 | 確認メール・スタッフ通知 |

---

## 日常オペレーション

```bash
# HPBからメールが来たとき（emails/ に .txt で保存してから）
npm run sync
# → 新規/キャンセル/変更を自動判定してR2キューを更新

# 空き枠データを手動同期（通常はVPSが1時間ごと自動実行）
npm run sync:slots

# VPS上でのworker起動（pm2で管理）
pm2 start "node worker.js --watch" --name hpb-worker
pm2 logs hpb-worker
```

## .env 設定項目

```
SALONBOARD_LOGIN_ID=xxx          # SalonBoard ログインID
SALONBOARD_PASSWORD=xxx          # SalonBoard パスワード
R2_ACCOUNT_ID=xxx                # Cloudflare R2 アカウントID
R2_ACCESS_KEY_ID=xxx             # R2 アクセスキー
R2_SECRET_ACCESS_KEY=xxx         # R2 シークレットキー
R2_BUCKET_NAME=xxx               # R2 バケット名
ADMIN_PASSWORD=xxx               # 管理画面パスワード
RESEND_API_KEY=xxx               # メール送信API
MAIL_FROM=xxx                    # 送信元アドレス
CRON_SECRET=xxx                  # Cron 認証トークン
STAFF_NOTIFY_EMAIL=xxx           # スタッフ通知先メール（省略可）
LINE_CHANNEL_ACCESS_TOKEN=xxx    # LINE Messaging API（省略可）
LINE_OWNER_USER_ID=xxx           # LINEオーナーID（省略可）
```
