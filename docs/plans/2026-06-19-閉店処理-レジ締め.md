# 閉店処理（レジ締め）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 閉店時にレジ金を金種別に数え、釣り銭準備金（固定繰越）＋当日現金売上から理論在高・過不足・入金額を算出して日次締めを `daily-close.json` に upsert する機能を内製化する。

**Architecture:** 会計フェーズと同じ「純粋計算コア（`lib/`）＋ Serverless API（`api/`）＋ 独立HTMLページ」構成。計算は `lib/close.js` に集約し `node:test` でテスト。`api/close.js` が `sales-log.json` から当日現金売上を再集計し締めを保存。UIは `report.html` をミラーした独立ページ `close.html`。

**Tech Stack:** Node.js 25（組込み `node:test`）, Cloudflare R2（`lib/storage.js`）, Vercel Serverless Functions, Express（ローカル `server.js`）, バニラJS（`close.html`）。

**設計書:** `docs/specs/2026-06-19-閉店処理-レジ締め-design.md`

---

## File Structure

| ファイル | 役割 | 新規/変更 |
|---|---|---|
| `lib/close.js` | 金種合計・理論在高・過不足・入金額の純粋関数 | 新規 |
| `test/close.test.js` | `lib/close.js` 単体テスト | 新規 |
| `api/close.js` | `GET /api/close?date=`（当日サマリ）／`POST /api/close`（締めupsert） | 新規 |
| `test/close.api.test.js` | `api/close.js` ハンドラテスト（mock storage） | 新規 |
| `close.html` | レジ締めページ（report.htmlミラー・金種UI・ライブ計算） | 新規 |
| `server.js` | `/close` ページルート追加 | 変更 |
| `vercel.json` | `/close` → `/close.html` ルート追加 | 変更 |
| `admin.html` / `report.html` | topnav に「🧾 レジ締め」リンク追加 | 変更 |
| `daily-close.json` | R2の締め台帳（コード生成・初回POSTで作成） | 実行時生成 |

---

## Task 1: 計算コア `lib/close.js`

**Files:**
- Create: `lib/close.js`
- Test: `test/close.test.js`

- [ ] **Step 1: Write the failing test**

`test/close.test.js`:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const { DENOMS, DEFAULT_FLOAT, countCash, computeClose } = require('../lib/close');

test('DENOMS は額面の降順10種', () => {
  assert.deepEqual(DENOMS, [10000, 5000, 2000, 1000, 500, 100, 50, 10, 5, 1]);
});

test('DEFAULT_FLOAT は 30000', () => {
  assert.equal(DEFAULT_FLOAT, 30000);
});

test('countCash は Σ(額面×枚数)', () => {
  assert.equal(countCash({ 10000: 4, 1000: 2, 500: 1, 100: 3 }), 42800);
  assert.equal(countCash({}), 0);
  assert.equal(countCash(null), 0);
});

test('countCash は不正値（負/NaN/小数）を0/floor扱い', () => {
  // 10000:-2→0, 5000:'x'→0, 1000:null→0, 500:1.9→floor1×500=500
  assert.equal(countCash({ 10000: -2, 5000: 'x', 1000: null, 500: 1.9 }), 500);
});

test('computeClose: 過不足0（実在高=理論在高）', () => {
  assert.deepEqual(
    computeClose({ float: 30000, cashSales: 12800, denominations: { 10000: 4, 1000: 2, 500: 1, 100: 3 } }),
    { countedCash: 42800, expectedCash: 42800, overShort: 0, deposit: 12800 });
});

test('computeClose: 不足はマイナス', () => {
  const r = computeClose({ float: 30000, cashSales: 12800, denominations: { 10000: 4 } });
  assert.equal(r.countedCash, 40000);
  assert.equal(r.expectedCash, 42800);
  assert.equal(r.overShort, -2800);
  assert.equal(r.deposit, 10000); // 40000 - 30000
});

test('computeClose: 過剰はプラス', () => {
  const r = computeClose({ float: 30000, cashSales: 10000, denominations: { 10000: 5 } });
  assert.equal(r.expectedCash, 40000);
  assert.equal(r.countedCash, 50000);
  assert.equal(r.overShort, 10000);
  assert.equal(r.deposit, 20000);
});

test('computeClose: deposit は負を0クランプ（実在高<準備金）', () => {
  const r = computeClose({ float: 30000, cashSales: 0, denominations: { 10000: 2 } });
  assert.equal(r.deposit, 0); // 20000 - 30000 → 0
});

test('computeClose: float/cashSales の不正値は0扱い', () => {
  const r = computeClose({ float: -5, cashSales: NaN, denominations: { 1000: 1 } });
  assert.equal(r.expectedCash, 0);
  assert.equal(r.countedCash, 1000);
  assert.equal(r.deposit, 1000);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/close.test.js`
Expected: FAIL（`Cannot find module '../lib/close'`）

- [ ] **Step 3: Write minimal implementation**

`lib/close.js`:
```js
/**
 * lib/close.js — レジ締めの純粋計算関数（副作用なし・ストレージ非依存）
 * 金種合計／理論在高／過不足／入金額を集約しテスト可能にする。
 */

const DENOMS = [10000, 5000, 2000, 1000, 500, 100, 50, 10, 5, 1];
const DEFAULT_FLOAT = 30000;

// 金種別枚数 → 実在高。負/NaN/未指定は0、小数は切り捨て。
function countCash(denominations) {
  const d = denominations || {};
  return DENOMS.reduce((sum, denom) => {
    const n = Number(d[denom]);
    return sum + (Number.isFinite(n) && n > 0 ? denom * Math.floor(n) : 0);
  }, 0);
}

// 締めの確定計算（純粋）
// 入力: { float, cashSales, denominations }
// 出力: { countedCash, expectedCash, overShort, deposit }
function computeClose({ float, cashSales, denominations }) {
  const f  = Math.max(0, Number(float) || 0);
  const cs = Math.max(0, Number(cashSales) || 0);
  const countedCash  = countCash(denominations);
  const expectedCash = f + cs;
  const overShort    = countedCash - expectedCash;
  const deposit      = Math.max(0, countedCash - f);
  return { countedCash, expectedCash, overShort, deposit };
}

module.exports = { DENOMS, DEFAULT_FLOAT, countCash, computeClose };
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/close.test.js`
Expected: PASS（9 tests）

- [ ] **Step 5: Commit**

```bash
git add lib/close.js test/close.test.js
git commit -m "feat: レジ締めの純粋計算コア lib/close.js を追加"
```

---

## Task 2: API `api/close.js`

**Files:**
- Create: `api/close.js`
- Test: `test/close.api.test.js`

- [ ] **Step 1: Write the failing test**

`test/close.api.test.js`（`test/checkout.api.test.js` と同じ storage モック方式）:
```js
const { test } = require('node:test');
const assert = require('node:assert');
const Module = require('module');

let stores;
const origLoad = Module._load;
Module._load = function (req) {
  if (req.endsWith('storage') || req === '../lib/storage') {
    return {
      get: async (k) => (k in stores ? stores[k] : []),
      put: async (k, body) => { stores[k] = JSON.parse(body); },
    };
  }
  return origLoad.apply(this, arguments);
};
process.env.COMINGSOON_PASSWORD = 'pw';
const handler = require('../api/close');

function mockRes() {
  return { _c: 200, _j: null, setHeader() {}, status(c){this._c=c;return this;}, json(o){this._j=o;return this;}, end(){return this;} };
}
const AUTH = { authorization: 'Bearer pw' };

function seed() {
  stores = {
    'sales-log.json': [
      { date: '2026-06-19', payment: 'cash', total: 5000 },
      { date: '2026-06-19', payment: 'cash', total: 7800 },
      { date: '2026-06-19', payment: 'card', total: 8000 },
      { date: '2026-06-18', payment: 'cash', total: 9999 }, // 別日は除外
    ],
    'daily-close.json': [
      { date: '2026-06-15', float: 25000 }, // 過去締め（defaultFloat 復元用）
    ],
  };
}

test('GET: 当日の現金/カード/QR集計と defaultFloat（最新締め）', async () => {
  seed();
  const res = mockRes();
  await handler({ method: 'GET', headers: AUTH, query: { date: '2026-06-19' } }, res);
  assert.equal(res._c, 200);
  assert.equal(res._j.cashSales, 12800);
  assert.equal(res._j.cardSales, 8000);
  assert.equal(res._j.qrSales, 0);
  assert.equal(res._j.defaultFloat, 25000); // 最新締め(6/15)のfloat
  assert.equal(res._j.existing, null);
});

test('GET: 既存締めがあれば existing とその float を返す', async () => {
  seed();
  stores['daily-close.json'].push({ date: '2026-06-19', float: 30000, denominations: { 10000: 1 } });
  const res = mockRes();
  await handler({ method: 'GET', headers: AUTH, query: { date: '2026-06-19' } }, res);
  assert.equal(res._j.defaultFloat, 30000);
  assert.equal(res._j.existing.denominations[10000], 1);
});

test('GET: 締め記録ゼロなら defaultFloat は DEFAULT_FLOAT(30000)', async () => {
  stores = { 'sales-log.json': [], 'daily-close.json': [] };
  const res = mockRes();
  await handler({ method: 'GET', headers: AUTH, query: { date: '2026-06-19' } }, res);
  assert.equal(res._j.defaultFloat, 30000);
});

test('GET: date 無しは400', async () => {
  seed();
  const res = mockRes();
  await handler({ method: 'GET', headers: AUTH, query: {} }, res);
  assert.equal(res._c, 400);
});

test('POST: サーバーで現金売上を再集計し締めを保存', async () => {
  seed();
  const res = mockRes();
  await handler({ method: 'POST', headers: AUTH, body: {
    date: '2026-06-19', float: 30000,
    denominations: { 10000: 4, 1000: 2, 500: 1, 100: 3 }, note: 'ok',
  }}, res);
  assert.equal(res._c, 200);
  assert.equal(res._j.cashSales, 12800);     // サーバー再集計（クライアント値は使わない）
  assert.equal(res._j.expectedCash, 42800);
  assert.equal(res._j.countedCash, 42800);
  assert.equal(res._j.overShort, 0);
  assert.equal(res._j.deposit, 12800);
  assert.equal(res._j.cardSales, 8000);
  assert.equal(res._j.note, 'ok');
  const saved = stores['daily-close.json'].find(c => c.date === '2026-06-19');
  assert.ok(saved);
  assert.equal(saved.countedCash, 42800);
});

test('POST: 同日は upsert で置換（重複しない）', async () => {
  seed();
  const before = stores['daily-close.json'].length;
  await handler({ method: 'POST', headers: AUTH, body: { date: '2026-06-19', float: 30000, denominations: { 10000: 4 } } }, mockRes());
  await handler({ method: 'POST', headers: AUTH, body: { date: '2026-06-19', float: 30000, denominations: { 10000: 5 } } }, mockRes());
  const same = stores['daily-close.json'].filter(c => c.date === '2026-06-19');
  assert.equal(same.length, 1);
  assert.equal(same[0].denominations[10000], 5);
  assert.equal(stores['daily-close.json'].length, before + 1);
});

test('POST: denominations 欠落は400', async () => {
  seed();
  const res = mockRes();
  await handler({ method: 'POST', headers: AUTH, body: { date: '2026-06-19', float: 30000 } }, res);
  assert.equal(res._c, 400);
});

test('認証なしは401', async () => {
  seed();
  const res = mockRes();
  await handler({ method: 'GET', headers: {}, query: { date: '2026-06-19' } }, res);
  assert.equal(res._c, 401);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `node --test test/close.api.test.js`
Expected: FAIL（`Cannot find module '../api/close'`）

- [ ] **Step 3: Write minimal implementation**

`api/close.js`:
```js
/**
 * api/close.js — レジ締め（日次）
 * GET  /api/close?date=YYYY-MM-DD → 当日サマリ（現金/カード/QR売上・defaultFloat・既存締め）
 * POST /api/close                 → 締め確定（daily-close.json に date キーで upsert）
 * 認証: Authorization: Bearer ${COMINGSOON_PASSWORD}
 */
require('dotenv').config();
const storage = require('../lib/storage');
const { computeClose, DEFAULT_FLOAT } = require('../lib/close');

const SALES_KEY = 'sales-log.json';
const CLOSE_KEY = 'daily-close.json';

const amountOf = (s) => (typeof s.total === 'number' ? s.total : (s.finalPrice || 0));

// 当日の支払方法別売上合計
function salesByPayment(sales, date) {
  const acc = { cash: 0, card: 0, qr: 0 };
  for (const s of sales) {
    if (s.date !== date) continue;
    const p = s.payment || 'cash';
    if (p in acc) acc[p] += amountOf(s);
  }
  return acc;
}

module.exports = async function handler(req, res) {
  const origin = req.headers.origin || '';
  if (origin) res.setHeader('Access-Control-Allow-Origin', origin);
  res.setHeader('Access-Control-Allow-Methods', 'GET, POST, OPTIONS');
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
  if (req.method === 'OPTIONS') return res.status(200).end();

  const authHeader = req.headers['authorization'] || '';
  if (authHeader !== `Bearer ${(process.env.COMINGSOON_PASSWORD || '').trim()}`) {
    return res.status(401).json({ error: '認証エラー' });
  }

  try {
    let sales = [];
    try { sales = (await storage.get(SALES_KEY)) || []; } catch { /* 未存在は空 */ }
    let closes = [];
    try { closes = (await storage.get(CLOSE_KEY)) || []; } catch { /* 未存在は空 */ }

    if (req.method === 'GET') {
      const date = req.query && req.query.date;
      if (!date) return res.status(400).json({ error: 'date は必須です' });
      const pay = salesByPayment(sales, date);
      const existing = closes.find(c => c.date === date) || null;
      // defaultFloat: 当日 → 最新締め → 定数 の3段フォールバック
      let defaultFloat = DEFAULT_FLOAT;
      if (existing) {
        defaultFloat = existing.float;
      } else if (closes.length) {
        const latest = closes.slice().sort((a, b) => (a.date < b.date ? 1 : -1))[0];
        defaultFloat = latest.float;
      }
      return res.status(200).json({
        date, existing,
        cashSales: pay.cash, cardSales: pay.card, qrSales: pay.qr,
        defaultFloat,
      });
    }

    if (req.method === 'POST') {
      const { date, float, denominations, note } = req.body || {};
      if (!date || denominations == null) {
        return res.status(400).json({ error: 'date と denominations は必須です' });
      }
      const pay = salesByPayment(sales, date);
      const calc = computeClose({ float, cashSales: pay.cash, denominations });
      const record = {
        date,
        float: Math.max(0, Number(float) || 0),
        cashSales: pay.cash,
        expectedCash: calc.expectedCash,
        denominations,
        countedCash: calc.countedCash,
        overShort: calc.overShort,
        deposit: calc.deposit,
        cardSales: pay.card,
        qrSales: pay.qr,
        note: note || '',
        closedAt: new Date().toISOString(),
      };
      const idx = closes.findIndex(c => c.date === date);
      if (idx >= 0) closes[idx] = record; else closes.push(record);
      await storage.put(CLOSE_KEY, JSON.stringify(closes));
      return res.status(200).json(record);
    }

    return res.status(405).json({ error: 'Method not allowed' });
  } catch (e) {
    console.error('close API error:', e);
    return res.status(500).json({ error: e.message });
  }
};
```

- [ ] **Step 4: Run test to verify it passes**

Run: `node --test test/close.api.test.js`
Expected: PASS（8 tests）

- [ ] **Step 5: Run the full suite to check no regression**

Run: `node --test test/*.test.js`
Expected: PASS（既存33件 + 新規17件 = 50件）

- [ ] **Step 6: Commit**

```bash
git add api/close.js test/close.api.test.js
git commit -m "feat: レジ締めAPI api/close.js（当日集計GET・締めupsert POST）を追加"
```

---

## Task 3: レジ締めページ `close.html` ＋ ルート ＋ ナビ

UIのため単体テストはなし。ローカル `server.js` ＋ ブラウザで動作確認する。

**Files:**
- Create: `close.html`
- Modify: `server.js`（`/close` ルート追加）
- Modify: `vercel.json`（`/close` ルート追加）
- Modify: `admin.html`（topnav にリンク追加）
- Modify: `report.html`（topnav にリンク追加）

- [ ] **Step 1: `close.html` を作成**

`close.html`（`report.html` のログイン/認証構造をミラー）:
```html
<!DOCTYPE html>
<html lang="ja">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>MONTH COLOR — レジ締め</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { font-family: system-ui, sans-serif; background: #f5f6f8; color: #222; }
  .topbar { background: #2d6a31; color: #fff; height: 52px; display: flex; align-items: center; padding: 0 18px; gap: 16px; }
  .topbar h1 { font-size: 16px; font-weight: 700; }
  .topbar a { color: #cde9d0; text-decoration: none; font-size: 13px; }
  .wrap { max-width: 560px; margin: 24px auto; padding: 0 16px; }
  .card { background: #fff; border-radius: 10px; padding: 20px; box-shadow: 0 1px 4px rgba(0,0,0,.06); }
  .login { max-width: 320px; margin: 80px auto; }
  .login input { width: 100%; padding: 10px; border: 1px solid #ccc; border-radius: 6px; margin: 8px 0; }
  .login button, .btn { background: #2d6a31; color: #fff; border: none; padding: 10px 18px; border-radius: 7px; cursor: pointer; font-size: 14px; }
  .datenav { display: flex; align-items: center; gap: 10px; margin-bottom: 16px; }
  .datenav h2 { font-size: 17px; flex: 1; }
  .datenav button { background: #eef0f2; border: 1px solid #ddd; border-radius: 6px; padding: 6px 10px; cursor: pointer; }
  .row { display: flex; justify-content: space-between; align-items: center; padding: 7px 0; }
  .row.total { border-top: 2px solid #222; margin-top: 6px; padding-top: 10px; font-weight: 700; font-size: 17px; }
  .muted { color: #888; font-size: 13px; }
  .float-in { width: 120px; padding: 6px 8px; border: 1px solid #ccc; border-radius: 6px; text-align: right; }
  table.denoms { width: 100%; border-collapse: collapse; margin: 12px 0; }
  table.denoms td { padding: 5px 4px; border-bottom: 1px solid #f0f0f0; }
  table.denoms .lbl { width: 90px; color: #555; }
  table.denoms .cnt input { width: 70px; padding: 5px 6px; border: 1px solid #ccc; border-radius: 5px; text-align: right; }
  table.denoms .sub { text-align: right; color: #333; width: 110px; }
  .oversh { font-weight: 700; }
  .oversh.zero { color: #1a7f37; }
  .oversh.short { color: #c00; }
  .oversh.over { color: #1565c0; }
  .note-in { width: 100%; padding: 8px; border: 1px solid #ccc; border-radius: 6px; margin-top: 4px; }
  .confirm-row { text-align: right; margin-top: 18px; }
  #toast { position: fixed; bottom: 20px; left: 50%; transform: translateX(-50%); background: #222; color: #fff; padding: 10px 18px; border-radius: 8px; opacity: 0; transition: opacity .2s; }
  #toast.show { opacity: 1; }
  #toast.err { background: #c00; }
</style>
</head>
<body>
  <div id="loginScreen" class="login">
    <div class="card">
      <h2 style="margin-bottom:8px">🧾 レジ締め</h2>
      <input type="password" id="loginPw" placeholder="パスワード">
      <button onclick="doLogin()">ログイン</button>
      <div id="loginError" style="color:#c00;font-size:13px;margin-top:6px"></div>
    </div>
  </div>

  <div id="mainScreen" style="display:none">
    <div class="topbar">
      <h1>MONTH COLOR</h1>
      <a href="/admin">← 予約管理</a>
      <a href="/report">📊 レポート</a>
      <span style="flex:1"></span>
      <a href="#" onclick="doLogout();return false">ログアウト</a>
    </div>
    <div class="wrap">
      <div class="card">
        <div class="datenav">
          <button onclick="shiftDay(-1)">◀ 前日</button>
          <button onclick="goToday()">今日</button>
          <button onclick="shiftDay(1)">翌日 ▶</button>
          <h2 id="dateLabel" style="text-align:right"></h2>
        </div>

        <div class="row"><span>釣り銭準備金</span>
          <input type="number" id="floatIn" class="float-in" min="0" oninput="recalc()"></div>
        <div class="row"><span>当日現金売上</span><span id="cashSales">¥0</span></div>
        <div class="row total"><span>理論在高</span><span id="expectedCash">¥0</span></div>

        <h3 style="margin:16px 0 4px;font-size:14px">金種別カウント</h3>
        <table class="denoms"><tbody id="denomBody"></tbody></table>
        <div class="row total"><span>実在高</span><span id="countedCash">¥0</span></div>

        <div class="row"><span>過不足</span><span id="overShort" class="oversh zero">±0</span></div>
        <div class="row"><span>入金額（繰越除く）</span><span id="deposit">¥0</span></div>

        <div style="margin-top:12px">
          <label class="muted">メモ</label>
          <input type="text" id="noteIn" class="note-in" placeholder="任意">
        </div>

        <div class="row muted" style="margin-top:10px">
          <span>参考</span><span>カード <b id="cardSales">¥0</b> / QR <b id="qrSales">¥0</b></span>
        </div>

        <div class="confirm-row"><button class="btn" onclick="confirmClose()">締め確定</button></div>
      </div>
    </div>
  </div>

  <div id="toast"></div>

<script>
const DENOMS = [10000, 5000, 2000, 1000, 500, 100, 50, 10, 5, 1];
let token = '';
let viewDate = new Date(); viewDate.setHours(0,0,0,0);
let serverCash = 0;

function pad(n){ return String(n).padStart(2,'0'); }
function dateStr(d){ return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}`; }
function fmt(n){ return `¥${(Number(n)||0).toLocaleString()}`; }
function toast(msg, err){ const t=document.getElementById('toast'); t.textContent=msg; t.className='show'+(err?' err':''); setTimeout(()=>t.className='',2000); }

async function doLogin(){
  const pw = document.getElementById('loginPw').value.trim();
  if (!pw) return;
  const r = await fetch(`/api/close?date=${dateStr(viewDate)}`, { headers: { Authorization: `Bearer ${pw}` } });
  if (!r.ok) { document.getElementById('loginError').textContent = 'パスワードが違います'; return; }
  token = pw;
  document.getElementById('loginScreen').style.display = 'none';
  document.getElementById('mainScreen').style.display = 'block';
  buildDenomRows();
  applyData(await r.json());
}
function doLogout(){ token=''; document.getElementById('mainScreen').style.display='none'; document.getElementById('loginScreen').style.display='block'; }
document.getElementById('loginPw').addEventListener('keydown', e => { if (e.key==='Enter') doLogin(); });

function buildDenomRows(){
  const tb = document.getElementById('denomBody');
  tb.innerHTML = DENOMS.map(d => `
    <tr>
      <td class="lbl">¥${d.toLocaleString()}</td>
      <td class="cnt"><input type="number" min="0" id="d_${d}" value="0" oninput="recalc()"></td>
      <td class="sub" id="s_${d}">¥0</td>
    </tr>`).join('');
}

function getDenoms(){
  const o = {};
  for (const d of DENOMS) o[d] = Number(document.getElementById(`d_${d}`).value) || 0;
  return o;
}

function recalc(){
  let counted = 0;
  for (const d of DENOMS) {
    const n = Math.max(0, Math.floor(Number(document.getElementById(`d_${d}`).value) || 0));
    const sub = d * n;
    counted += sub;
    document.getElementById(`s_${d}`).textContent = fmt(sub);
  }
  const float = Math.max(0, Number(document.getElementById('floatIn').value) || 0);
  const expected = float + serverCash;
  const over = counted - expected;
  const deposit = Math.max(0, counted - float);
  document.getElementById('expectedCash').textContent = fmt(expected);
  document.getElementById('countedCash').textContent = fmt(counted);
  document.getElementById('deposit').textContent = fmt(deposit);
  const el = document.getElementById('overShort');
  el.textContent = over === 0 ? '±0' : (over > 0 ? `+${fmt(over)}` : `-${fmt(-over)}`);
  el.className = 'oversh ' + (over === 0 ? 'zero' : over < 0 ? 'short' : 'over');
}

function applyData(data){
  document.getElementById('dateLabel').textContent = data.date;
  serverCash = data.cashSales || 0;
  document.getElementById('cashSales').textContent = fmt(data.cashSales);
  document.getElementById('cardSales').textContent = fmt(data.cardSales);
  document.getElementById('qrSales').textContent = fmt(data.qrSales);
  document.getElementById('floatIn').value = data.defaultFloat;
  const ex = data.existing;
  for (const d of DENOMS) document.getElementById(`d_${d}`).value = (ex && ex.denominations && ex.denominations[d]) || 0;
  document.getElementById('noteIn').value = (ex && ex.note) || '';
  recalc();
}

async function loadClose(){
  const r = await fetch(`/api/close?date=${dateStr(viewDate)}`, { headers: { Authorization: `Bearer ${token}` } });
  if (!r.ok) { toast('取得失敗', true); return; }
  applyData(await r.json());
}
function shiftDay(delta){ viewDate = new Date(viewDate.getTime() + delta*86400000); loadClose(); }
function goToday(){ viewDate = new Date(); viewDate.setHours(0,0,0,0); loadClose(); }

async function confirmClose(){
  const body = { date: dateStr(viewDate), float: Number(document.getElementById('floatIn').value) || 0, denominations: getDenoms(), note: document.getElementById('noteIn').value };
  const r = await fetch('/api/close', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify(body) });
  const j = await r.json();
  if (!r.ok) { toast('締めエラー: ' + (j.error || r.status), true); return; }
  toast('締めを保存しました');
  applyData({ date: j.date, cashSales: j.cashSales, cardSales: j.cardSales, qrSales: j.qrSales, defaultFloat: j.float, existing: j });
}
</script>
</body>
</html>
```

- [ ] **Step 2: `server.js` に `/close` ページルートを追加**

`server.js` の既存ページルート群（`app.get('/reserve', ...)` の並び、約44-48行目付近）に1行追加:
```js
app.get('/close',     (_, res) => res.sendFile(path.join(__dirname, 'close.html')));
```

- [ ] **Step 3: `vercel.json` に `/close` ルートを追加**

`vercel.json` の `routes` 配列、`{ "src": "/reserve", "dest": "/reservation.html" }` の直後に追加:
```json
    { "src": "/close",      "dest": "/close.html" },
```

- [ ] **Step 4: `admin.html` の topnav にリンクを追加**

`admin.html` の `<div class="topnav__tabs">` 内、レポートタブ（`<a href="/report" ...>` のブロック）の直後に追加:
```html
      <a href="/close" style="text-decoration:none">
        <button class="topnav__tab"><span>🧾 レジ締め</span></button>
      </a>
```

- [ ] **Step 5: `report.html` の topnav にリンクを追加**

`report.html` のヘッダーナビ（`<h1>MONTH COLOR</h1>` 付近のリンク群）に、他ページへのリンクと並べて追加:
```html
      <a href="/close">🧾 レジ締め</a>
```
（report.html のナビ構造に合わせて配置。リンクが無ければ `<h1>` の直後に `<a href="/close" style="color:#cde9d0;text-decoration:none;font-size:13px;margin-left:12px">🧾 レジ締め</a>` を追加）

- [ ] **Step 6: ブラウザ実機確認**

1. サーバー起動: `PORT=3016 node server.js`
2. Playwright もしくは手動で `http://localhost:3016/close` を開く
3. パスワードでログイン → 当日のレジ締め画面が表示される
4. 金種に枚数を入力 → 実在高・過不足（色）・入金額がライブ更新される
5. 釣り銭準備金を変更 → 理論在高・過不足が追従
6. 前日/翌日ナビ → `GET /api/close` で日付が切り替わる
7. （任意・本番R2書込）「締め確定」→ トースト表示 → 再ロードで金種が復元される
8. サーバー停止

確認用 Playwright スクリプトは `_temp/`（gitignore済）に置く。`confirmClose` は本番R2書込のため、実機ではライブ計算・復元までを確認し、書込はAPIテストでカバー（または当日テスト用に1回のみ実行）。

- [ ] **Step 7: Commit**

```bash
git add close.html server.js vercel.json admin.html report.html
git commit -m "feat: レジ締めページ close.html とナビ/ルートを追加"
```

---

## 完了後

- ブランチ `feat/close-inhouse` を master へマージ（`--no-ff`、finishing-a-development-branch）
- `BRIEF.md` の「次のアクション」を更新（閉店処理フェーズ完了・次は月次/集計拡充など）
- mistakes.md 追記は不要（新規パターンの罠があれば追記）
