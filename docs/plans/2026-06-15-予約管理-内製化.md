# 予約管理の内製化（1cs相当の予約登録） Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 1cs相当の予約登録（カナ・複数メニュー単価付き・経路・状態）を admin に内製化し、予約を自前R2に保存しつつ SalonBoard へ枠ブロック送信する。

**Architecture:** 純粋ロジック（CSV変換・所要時間/空き枠計算）は `lib/` と `api/_shared.js` に置き `node --test` で単体テスト。メニューマスタは CSV→`menu-master.json`（R2）。`POST /api/reserve` を拡張して自前R2を正としつつ `status=pending` で SalonBoard 送信キューにも積む（worker.js は既存のまま）。admin.html のフォーム/一覧を拡張。

**Tech Stack:** Node.js 25（組込み `node:test`）, Cloudflare R2（`lib/storage.js`）, Vercel Serverless Functions, バニラJS（admin.html）, Playwright（既存worker）。

**設計書:** `docs/specs/2026-06-15-予約管理-内製化-design.md`

---

## File Structure

| ファイル | 役割 | 新規/変更 |
|---|---|---|
| `lib/menu.js` | CSV(CP932)テキスト → メニュー配列（service/durationMin付与）の純粋関数 | 新規 |
| `test/menu.test.js` | `lib/menu.js` の単体テスト | 新規 |
| `scripts/import_menu_master.js` | CSVファイルを読み `lib/menu.js` で変換し `menu-master.json` をR2へ保存 | 新規 |
| `api/menu.js` | `GET /api/menu` — `menu-master.json` を配信 | 新規 |
| `api/_shared.js` | `getOccupiedSlotsForItems` / `serviceForCategory` / `durationForMenu` 追加 | 変更 |
| `test/_shared.test.js` | `_shared` 追加関数の単体テスト | 新規 |
| `api/reserve.js` | カナ・menuItems・source・nominated・customerId・visitStatus 対応 | 変更 |
| `test/reserve.test.js` | `reserve` ハンドラのケーステスト（mock req/res） | 新規 |
| `admin.html` | 予約登録フォーム拡張（カナ・顧客検索・メニュー選択）＋プレート一覧の状態/経路/指名表示 | 変更 |

**カテゴリ→サービス区分／所要時間ルール（`lib/menu.js` と `_shared` で共有）:**
- service: 技術分類に「ホワイトニング」→`white` /「まつ毛 or まつげ」→`lash` /「ヘッドスパ」→`spa` /「割引 or 補正」→`null` / その他→`hair`
- durationMin: 技術名に `／(\d+)分／` があればその分。無ければ分類デフォルト（リタッチ70 / 全体染めショート・ミディアム90・ロング/ダブルロング120 / トリートメント30 / カラー＋TR系90 / その他ヘアカラー60 / まつ毛60 / ヘッドスパ60 / その他60）。service=null（割引/補正）は durationMin=0。

---

## Task 1: メニューCSV変換の純粋関数 `lib/menu.js`

**Files:**
- Create: `lib/menu.js`
- Test: `test/menu.test.js`

- [ ] **Step 1: 失敗するテストを書く**

```js
// test/menu.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const { parseMenuCsv, serviceForCategory, durationForMenu } = require('../lib/menu');

test('serviceForCategory はキーワードで区分を返す', () => {
  assert.equal(serviceForCategory('根元染め(リタッチ)'), 'hair');
  assert.equal(serviceForCategory('セルフホワイトニング'), 'white');
  assert.equal(serviceForCategory('まつ毛パーマ'), 'lash');
  assert.equal(serviceForCategory('ドライヘッドスパ'), 'spa');
  assert.equal(serviceForCategory('割引'), null);
  assert.equal(serviceForCategory('補正'), null);
});

test('durationForMenu は名前の「N分」を優先し、無ければ分類デフォルト', () => {
  assert.equal(durationForMenu('セルフホワイトニング', 'セルフホワイトニング 60分'), 60);
  assert.equal(durationForMenu('根元染め(リタッチ)', '【平日通常】リタッチ'), 70);
  assert.equal(durationForMenu('割引', '【友達紹介】割引'), 0);
});

test('parseMenuCsv はヘッダ行を捨てて service/durationMin 付きの配列を返す', () => {
  const csv = [
    '"技術コード","技術分類","技術名","税込技術料","税抜技術料","基本販売数量","レジ表示"',
    '"T010001","根元染め(リタッチ)","【平日通常】リタッチ","2980","2710","1",""',
    '"W010002","セルフホワイトニング","セルフホワイトニング 60分","6600","6000","1",""',
  ].join('\n');
  const rows = parseMenuCsv(csv);
  assert.equal(rows.length, 2);
  assert.deepEqual(rows[0], {
    code: 'T010001', category: '根元染め(リタッチ)', name: '【平日通常】リタッチ',
    priceIncTax: 2980, priceExTax: 2710, defaultQty: 1, service: 'hair', durationMin: 70,
  });
  assert.equal(rows[1].service, 'white');
  assert.equal(rows[1].durationMin, 60);
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `node --test test/menu.test.js`
Expected: FAIL（`Cannot find module '../lib/menu'`）

- [ ] **Step 3: `lib/menu.js` を実装**

```js
// lib/menu.js — メニューCSV(CP932)テキストを構造化する純粋関数群

const CATEGORY_DEFAULT_DURATION = [
  [/リタッチ|根元染め/, 70],
  [/全体染め.*(ロング|ダブルロング)/, 120],
  [/全体染め/, 90],
  [/カラー＋|カラーキープ|プレミアム/, 90],
  [/トリートメント/, 30],
  [/まつ[毛げ]/, 60],
  [/ヘッドスパ/, 60],
];

function serviceForCategory(category) {
  const s = (category || '').replace(/[\s　]/g, '');
  if (/割引|補正/.test(s)) return null;
  if (/ホワイトニング/.test(s)) return 'white';
  if (/まつ[毛げ]/.test(s)) return 'lash';
  if (/ヘッドスパ/.test(s)) return 'spa';
  return 'hair';
}

function durationForMenu(category, name) {
  if (serviceForCategory(category) === null) return 0;
  const m = (name || '').match(/(\d+)分/);
  if (m) return parseInt(m[1], 10);
  for (const [re, dur] of CATEGORY_DEFAULT_DURATION) {
    if (re.test(category || '')) return dur;
  }
  return 60;
}

// CSV1行を "..." 区切りでパース（フィールド内カンマは想定しない単純CSV）
function parseCsvLine(line) {
  return [...line.matchAll(/"((?:[^"])*)"/g)].map(m => m[1]);
}

function parseMenuCsv(csvText) {
  const lines = csvText.replace(/\r\n/g, '\n').trim().split('\n');
  const out = [];
  for (let i = 1; i < lines.length; i++) {            // 0行目はヘッダ
    const c = parseCsvLine(lines[i]);
    if (c.length < 5 || !c[0]) continue;
    const category = c[1];
    const name = c[2];
    out.push({
      code: c[0],
      category,
      name,
      priceIncTax: parseInt(c[3], 10) || 0,
      priceExTax: parseInt(c[4], 10) || 0,
      defaultQty: parseInt(c[5], 10) || 1,
      service: serviceForCategory(category),
      durationMin: durationForMenu(category, name),
    });
  }
  return out;
}

module.exports = { parseMenuCsv, serviceForCategory, durationForMenu };
```

- [ ] **Step 4: テストを実行して成功を確認**

Run: `node --test test/menu.test.js`
Expected: PASS（3 tests）

- [ ] **Step 5: コミット**

```bash
git add lib/menu.js test/menu.test.js
git commit -m "feat: メニューCSV変換の純粋関数lib/menuを追加（service/duration付与）"
```

---

## Task 2: メニューマスタ取り込みスクリプト `scripts/import_menu_master.js`

**Files:**
- Create: `scripts/import_menu_master.js`

- [ ] **Step 1: スクリプトを実装**（CP932デコードは Node の `TextDecoder('shift_jis')` を使用）

```js
// scripts/import_menu_master.js
// 使い方: node scripts/import_menu_master.js "_temp/items_MONTH COLOR_20260615.csv"
//        --preview を付けると保存せず先頭を表示
require('dotenv').config();
const fs = require('fs');
const storage = require('../lib/storage');
const { parseMenuCsv } = require('../lib/menu');

const MENU_KEY = 'menu-master.json';

(async () => {
  const file = process.argv[2];
  const preview = process.argv.includes('--preview');
  if (!file) { console.error('CSVパスを渡してください'); process.exit(1); }

  const buf = fs.readFileSync(file);
  const text = new TextDecoder('shift_jis').decode(buf);
  const rows = parseMenuCsv(text);
  console.log(`変換: ${rows.length}件 / 分類 ${new Set(rows.map(r => r.category)).size}種`);

  if (preview) { console.log(JSON.stringify(rows.slice(0, 5), null, 2)); return; }
  await storage.put(MENU_KEY, JSON.stringify(rows, null, 2));
  console.log(`✅ ${MENU_KEY} に保存しました`);
})().catch(e => { console.error('FATAL:', e.message); process.exit(1); });
```

- [ ] **Step 2: preview で件数を確認**

Run: `node scripts/import_menu_master.js "_temp/items_MONTH COLOR_20260615.csv" --preview`
Expected: `変換: 63件 / 分類 14種` と先頭5件のJSON（`service`/`durationMin`付き）が表示される

- [ ] **Step 3: 本番保存（R2へ）**

Run: `node scripts/import_menu_master.js "_temp/items_MONTH COLOR_20260615.csv"`
Expected: `✅ menu-master.json に保存しました`

- [ ] **Step 4: コミット**

```bash
git add scripts/import_menu_master.js
git commit -m "feat: メニューCSV→menu-master.json 取り込みスクリプトを追加"
```

---

## Task 3: メニュー配信API `GET /api/menu`

**Files:**
- Create: `api/menu.js`

- [ ] **Step 1: ハンドラを実装**

```js
// api/menu.js — GET /api/menu  menu-master.json を配信
const storage = require('../lib/storage');

module.exports = async function handler(req, res) {
  const origin = req.headers.origin || '';
  if (origin) res.setHeader('Access-Control-Allow-Origin', origin);
  res.setHeader('Access-Control-Allow-Methods', 'GET, OPTIONS');
  if (req.method === 'OPTIONS') return res.status(200).end();
  if (req.method !== 'GET') return res.status(405).json({ error: 'Method not allowed' });

  try {
    const menu = (await storage.get('menu-master.json')) || [];
    res.setHeader('Cache-Control', 'public, max-age=300');
    return res.status(200).json({ ok: true, menu });
  } catch (err) {
    console.error('menu取得エラー:', err.message);
    return res.status(200).json({ ok: false, menu: [] });
  }
};
```

- [ ] **Step 2: ローカル検証スクリプトで確認**

Run:
```bash
node -e "require('dotenv').config(); const h=require('./api/menu'); h({method:'GET',headers:{}}, {setHeader(){}, status(c){this._c=c;return this;}, json(o){console.log('status',this._c,'件数',o.menu.length);} , end(){}});"
```
Expected: `status 200 件数 63`

- [ ] **Step 3: コミット**

```bash
git add api/menu.js
git commit -m "feat: GET /api/menu でメニューマスタを配信"
```

---

## Task 4: `_shared.js` に複数メニュー対応の所要時間/空き枠ロジックを追加

**Files:**
- Modify: `api/_shared.js`
- Test: `test/_shared.test.js`

- [ ] **Step 1: 失敗するテストを書く**

```js
// test/_shared.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const { getOccupiedSlotsForItems, serviceForItems } = require('../api/_shared');

test('getOccupiedSlotsForItems は menuItems の合計所要時間で枠を返す', () => {
  // 70分(リタッチ)+30分(炭酸泉) = 100分 → 30分刻みで4枠
  const items = [{ durationMin: 70 }, { durationMin: 30 }];
  assert.deepEqual(getOccupiedSlotsForItems('10:00', items), ['10:00','10:30','11:00','11:30']);
});

test('getOccupiedSlotsForItems は空配列なら60分扱いで2枠', () => {
  assert.deepEqual(getOccupiedSlotsForItems('10:00', []), ['10:00','10:30']);
});

test('serviceForItems は最初の有効サービスを主区分にする', () => {
  assert.equal(serviceForItems([{ service: null }, { service: 'hair' }]), 'hair');
  assert.equal(serviceForItems([]), 'hair');
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `node --test test/_shared.test.js`
Expected: FAIL（`getOccupiedSlotsForItems is not a function`）

- [ ] **Step 3: `_shared.js` に関数を追加**（既存の `getOccupiedSlots` の直後に追記し、`module.exports` に追加）

```js
// 複数メニューの合計所要時間から占有スロットを返す
function getOccupiedSlotsForItems(timeStr, menuItems) {
  const totalMin = (menuItems || []).reduce((s, m) => s + (m.durationMin || 0), 0) || 60;
  const [h, m] = timeStr.split(':').map(Number);
  const startMin = h * 60 + m;
  const slotCount = Math.ceil(totalMin / 30);
  return Array.from({ length: slotCount }, (_, i) => {
    const t = startMin + i * 30;
    return `${String(Math.floor(t / 60)).padStart(2, '0')}:${String(t % 60).padStart(2, '0')}`;
  });
}

// menuItems から主サービス区分を決定（null=割引等は飛ばす）
function serviceForItems(menuItems) {
  for (const m of (menuItems || [])) {
    if (m.service) return m.service;
  }
  return 'hair';
}
```

`module.exports` を次のように更新（既存に2関数を追加）:

```js
module.exports = {
  CAPACITY, MENU_DURATION, ALL_TIMES, QUEUE_KEY,
  getOccupiedSlots, getOccupiedSlotsForItems, serviceForItems,
  normalizeCategory, buildBookedMap, loadQueue, menuToCategory,
};
```
> 注: 既存 `module.exports` に現在含まれている識別子は変更前のものを確認し、**漏れなく**引き継ぐこと（`CAPACITY`/`getOccupiedSlots`/`buildBookedMap`/`loadQueue`/`QUEUE_KEY` は `reserve.js` が import 済み）。

- [ ] **Step 4: テストを実行して成功を確認**

Run: `node --test test/_shared.test.js`
Expected: PASS（3 tests）

- [ ] **Step 5: コミット**

```bash
git add api/_shared.js test/_shared.test.js
git commit -m "feat: _sharedに複数メニュー合計の所要時間/主区分ロジックを追加"
```

---

## Task 5: `reserve.js` を拡張（カナ・menuItems・経路・状態・後方互換）

**Files:**
- Modify: `api/reserve.js`
- Test: `test/reserve.test.js`

- [ ] **Step 1: 失敗するテストを書く**（storageをスタブ）

```js
// test/reserve.test.js
const { test } = require('node:test');
const assert = require('node:assert');
const Module = require('module');

// lib/storage をスタブ（R2に触らない）
let saved = null;
const origLoad = Module._load;
Module._load = function (req, parent, isMain) {
  if (req.endsWith('storage') || req === '../lib/storage') {
    return { get: async () => [], put: async (_k, body) => { saved = JSON.parse(body); } };
  }
  return origLoad.apply(this, arguments);
};
const handler = require('../api/reserve');

function mockRes() {
  return { _c: 200, _j: null, setHeader() {}, status(c){this._c=c;return this;}, json(o){this._j=o;return this;}, end(){return this;} };
}

test('menuItems から service と占有枠を計算して保存する', async () => {
  saved = null;
  const req = { method: 'POST', headers: {}, body: {
    date: '2026-07-02', time: '10:00', name: 'テスト太郎', nameKana: 'てすとたろう',
    phone: '090-0000-0000', source: 'phone',
    menuItems: [{ code: 'T010001', name: '【平日通常】リタッチ', price: 2980, durationMin: 70, service: 'hair' }],
  }};
  const res = mockRes();
  await handler(req, res);
  assert.equal(res._c, 200);
  assert.ok(saved && saved.length === 1);
  assert.equal(saved[0].status, 'pending');
  assert.equal(saved[0].data.visitStatus, 'reserved');
  assert.equal(saved[0].data.service, 'hair');
  assert.equal(saved[0].data.nameKana, 'てすとたろう');
  assert.equal(saved[0].data.menuItems.length, 1);
});

test('必須欠落は400', async () => {
  const res = mockRes();
  await handler({ method: 'POST', headers: {}, body: { time: '10:00', name: 'x' } }, res);
  assert.equal(res._c, 400);
});
```

- [ ] **Step 2: テストを実行して失敗を確認**

Run: `node --test test/reserve.test.js`
Expected: FAIL（`visitStatus` 未設定 or `service` undefined）

- [ ] **Step 3: `reserve.js` を変更**

3-a. import に新関数を追加（13行目）:
```js
const { CAPACITY, getOccupiedSlots, getOccupiedSlotsForItems, serviceForItems, buildBookedMap, loadQueue, QUEUE_KEY } = require('./_shared');
```

3-b. 入力の分解（25行目）を拡張:
```js
  const {
    date, time, name, nameKana, phone, email, memo,
    menuItems, source, nominated, customerId,
    menuName, menuId, staffCategory,            // 後方互換（旧クライアント）
  } = req.body || {};

  if (!date || !time || !name) {
    return res.status(400).json({ error: '日付・時間・お名前は必須です' });
  }

  // menuItems があれば新方式、無ければ旧 menuId 単体から擬似 items を作る
  const items = Array.isArray(menuItems) && menuItems.length
    ? menuItems
    : (menuId ? [{ code: menuId, name: menuName || '', price: 0, durationMin: 0, service: null }] : []);
  const service = serviceForItems(items) || staffCategory && require('./_shared').normalizeCategory(staffCategory) || 'hair';
  const newSlots = items.length ? getOccupiedSlotsForItems(time, items) : getOccupiedSlots(time, menuId);
  const displayMenu = menuName || items.map(i => i.name).filter(Boolean).join('＆');
```

3-c. 満席チェック（37-44行目）を `service` ベースに:
```js
    const queue = await loadQueue();
    const cap = CAPACITY[service] || 1;
    const booked = buildBookedMap(queue, service);
    const fullSlot = newSlots.find(s => (booked[`${date}:${s}`] || 0) >= cap);
    if (fullSlot) {
      return res.status(409).json({ error: 'この時間帯はすでに満席です。別の時間帯をお選びください。' });
    }
```

3-d. 予約オブジェクト（47-54行目）を拡張:
```js
    const reservation = {
      id:        uuidv4(),
      status:    'pending',           // SalonBoard送信状態: pending|processing|completed|failed
      data: {
        date, time, name,
        nameKana: nameKana || '',
        customerId: customerId || (phone ? `phone:${phone.replace(/[^0-9]/g, '')}` : null),
        phone: phone || '', email: email || '', memo: memo || '',
        menuItems: items,
        menuName: displayMenu,        // 後方互換: worker/メール用の表示名
        menuId: menuId || (items[0] && items[0].code) || null,
        service,
        staffCategory: staffCategory || service,   // 後方互換
        source: source || 'manual',  // net|phone|walkin|manual|staff
        nominated: !!nominated,
        visitStatus: 'reserved',      // 来店管理: reserved→arrived→unpaid→paid|cancelled
      },
      createdAt: new Date().toISOString(),
      processedAt: null,
      error:     null,
    };
    queue.push(reservation);
```

3-e. メール/通知呼び出しは `menuName: displayMenu` を渡すよう既存の `menuName` 参照を `displayMenu` に統一（62-69行目の `menuName` を `displayMenu` に置換）。

- [ ] **Step 4: テストを実行して成功を確認**

Run: `node --test test/reserve.test.js`
Expected: PASS（2 tests）

- [ ] **Step 5: 全テストを通す**

Run: `node --test test/`
Expected: 全 PASS（menu / _shared / reserve）

- [ ] **Step 6: コミット**

```bash
git add api/reserve.js test/reserve.test.js
git commit -m "feat: reserveをカナ/menuItems/経路/状態(visitStatus)対応に拡張（後方互換維持）"
```

---

## Task 6: admin.html 予約登録フォームの拡張

**Files:**
- Modify: `admin.html`（新規予約モーダル `817-870` 付近、`submitNew()` `1308` 付近、フォーム初期化）

- [ ] **Step 1: カナ入力欄を追加**（モーダル `お名前` 行の直後）

```html
    <div class="form-row">
      <label>カナ <span style="color:#aaa;font-size:11px">任意</span></label>
      <input type="text" id="newNameKana" placeholder="例: やまだ はなこ">
    </div>
```

- [ ] **Step 2: 顧客検索（既存カルテ自動入力）を追加**（お名前欄の上に検索ボックス）

```html
    <div class="form-row">
      <label>顧客検索 <span style="color:#aaa;font-size:11px">電話/名前</span></label>
      <input type="text" id="newCustomerSearch" placeholder="電話番号 or 名前で検索" oninput="onCustomerSearch()">
      <div id="newCustomerResults" class="cust-results"></div>
    </div>
```

スクリプトに追記（`submitNew` の近く）:
```js
let _custTimer = null;
async function onCustomerSearch() {
  clearTimeout(_custTimer);
  const q = document.getElementById('newCustomerSearch').value.trim();
  const box = document.getElementById('newCustomerResults');
  if (q.length < 2) { box.innerHTML = ''; return; }
  _custTimer = setTimeout(async () => {
    const key = /^[0-9-]+$/.test(q) ? `phone=${encodeURIComponent(q)}` : `name=${encodeURIComponent(q)}`;
    const r = await fetch(`/api/customers?${key}`, { headers: { Authorization: `Bearer ${password}` } });
    const d = await r.json();
    const list = d.customers || (d.customer ? [d.customer] : []);
    box.innerHTML = list.slice(0, 5).map(c =>
      `<div class="cust-hit" onclick='fillCustomer(${JSON.stringify(c).replace(/'/g, "&#39;")})'>${c.name}（${c.phone||'—'}）<span>来店${c.visitCount||0}回</span></div>`
    ).join('');
  }, 250);
}
function fillCustomer(c) {
  document.getElementById('newName').value = c.name || '';
  document.getElementById('newPhone').value = c.phone || '';
  document.getElementById('newCustomerResults').innerHTML = '';
  document.getElementById('newCustomerSearch').value = '';
}
```

CSS（`<style>` 末尾に追記）:
```css
.cust-results{display:flex;flex-direction:column;gap:2px;margin-top:4px}
.cust-hit{padding:6px 8px;background:#f6f6f6;border-radius:6px;cursor:pointer;font-size:13px;display:flex;justify-content:space-between}
.cust-hit:hover{background:#eee}
.cust-hit span{color:#999;font-size:11px}
```

- [ ] **Step 3: メニュー選択をマスタ連動の複数選択に置き換え**（既存の自由記入 `newMenu` 行を置換）

```html
    <div class="form-row">
      <label>メニュー <span style="color:#c00">*</span></label>
      <select id="newMenuCategory" onchange="renderMenuOptions()"><option value="">分類を選択</option></select>
      <select id="newMenuPick" onchange="addMenuItem()"><option value="">メニューを選択</option></select>
      <div id="newMenuItems" class="menu-chips"></div>
      <div id="newMenuTotal" style="font-size:12px;color:#666;margin-top:4px"></div>
    </div>
```

スクリプト:
```js
let _menuMaster = [];
let _selectedMenu = [];
async function loadMenuMaster() {
  const r = await fetch('/api/menu'); const d = await r.json();
  _menuMaster = d.menu || [];
  const cats = [...new Set(_menuMaster.map(m => m.category))];
  document.getElementById('newMenuCategory').innerHTML =
    '<option value="">分類を選択</option>' + cats.map(c => `<option>${c}</option>`).join('');
}
function renderMenuOptions() {
  const cat = document.getElementById('newMenuCategory').value;
  const items = _menuMaster.filter(m => m.category === cat);
  document.getElementById('newMenuPick').innerHTML =
    '<option value="">メニューを選択</option>' +
    items.map(m => `<option value="${m.code}">${m.name}（¥${m.priceIncTax}）</option>`).join('');
}
function addMenuItem() {
  const code = document.getElementById('newMenuPick').value; if (!code) return;
  const m = _menuMaster.find(x => x.code === code); if (!m) return;
  _selectedMenu.push({ code: m.code, name: m.name, price: m.priceIncTax, durationMin: m.durationMin, service: m.service });
  renderSelectedMenu();
}
function removeMenuItem(i) { _selectedMenu.splice(i, 1); renderSelectedMenu(); }
function renderSelectedMenu() {
  document.getElementById('newMenuItems').innerHTML = _selectedMenu.map((m, i) =>
    `<span class="chip">${m.name} ¥${m.price}<b onclick="removeMenuItem(${i})">×</b></span>`).join('');
  const yen = _selectedMenu.reduce((s, m) => s + m.price, 0);
  const min = _selectedMenu.reduce((s, m) => s + (m.durationMin || 0), 0);
  document.getElementById('newMenuTotal').textContent = _selectedMenu.length ? `合計 ¥${yen} / 約${min}分` : '';
  if (typeof onNewSlotCondChange === 'function') onNewSlotCondChange();
}
```

CSS:
```css
.menu-chips{display:flex;flex-wrap:wrap;gap:4px;margin-top:6px}
.chip{background:#efe9df;border-radius:14px;padding:3px 10px;font-size:12px}
.chip b{margin-left:6px;cursor:pointer;color:#a00}
```

`openNewModal()` の中で `loadMenuMaster()` と `_selectedMenu=[]` を呼ぶよう追記。

- [ ] **Step 4: `submitNew()` を新ペイロードに更新**（`1308` 付近を置換）

```js
async function submitNew() {
  const date = document.getElementById('newDate').value;
  const time = document.getElementById('newTime').value;
  const name = document.getElementById('newName').value.trim();
  const nameKana = document.getElementById('newNameKana').value.trim();
  const phone = document.getElementById('newPhone').value.trim();
  const email = document.getElementById('newEmail').value.trim();
  const memo  = document.getElementById('newMemo').value.trim();

  if (!date) { showToast('日付を選択してください', true); return; }
  if (!time) { showToast('時間スロットを選択してください', true); return; }
  if (!name) { showToast('お名前を入力してください', true); return; }
  if (!phone) { showToast('電話番号を入力してください', true); return; }
  if (!_selectedMenu.length) { showToast('メニューを選択してください', true); return; }

  const btn = document.getElementById('newSubmitBtn');
  btn.disabled = true; btn.textContent = '登録中...';
  try {
    const res = await fetch('/api/reserve', {
      method: 'POST', headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ date, time, name, nameKana, phone, email, memo,
        menuItems: _selectedMenu, source: 'staff' }),
    });
    const data = await res.json();
    if (!res.ok) { showToast(`エラー: ${data.error || '登録失敗'}`, true); return; }
    closeNewModal(); showToast('✅ 予約を登録しました'); await reload();
  } catch { showToast('通信エラーが発生しました', true); }
  finally { btn.disabled = false; btn.textContent = '予約を登録'; }
}
```

> 注: 空き枠グリッド（`onNewSlotCondChange`/`renderNewSlots`）は `service` を `_selectedMenu[0].service` から決定するよう、既存の `newServiceType` 参照箇所を `_selectedMenu[0]?.service` にフォールバックさせる。`newServiceType` セレクトは残してもよいが、メニュー選択時は自動で区分が決まる。

- [ ] **Step 5: ローカルで手動確認**

Run: `node server.js`（ローカルExpress）→ ブラウザで admin を開く
確認:
1. 「＋新規予約」→ 分類選択→メニュー選択→チップ追加→合計表示
2. 顧客検索で既存名がヒットし、クリックで氏名・電話が入る
3. 登録 → トースト「✅ 予約を登録しました」→ 一覧に反映

- [ ] **Step 6: コミット**

```bash
git add admin.html
git commit -m "feat: admin予約登録をカナ・顧客検索・メニューマスタ複数選択に拡張"
```

---

## Task 7: admin.html 予約プレート一覧の状態/経路/指名表示

**Files:**
- Modify: `admin.html`（`renderCards()` と状態ラベル定義）

- [ ] **Step 1: 来店管理状態ラベルを追加**（`STATUS_LABEL` の近く）

```js
const VISIT_LABEL = {
  reserved: '📅 予約', arrived: '🟢 来店', unpaid: '💰 未精算',
  paid: '✅ 精算済', cancelled: '🚫 キャンセル',
};
const SOURCE_LABEL = { net: '🌐ネット', phone: '📞電話', walkin: '🚶店頭', staff: '🧑‍💼手動', manual: '✍️手動' };
```

- [ ] **Step 2: `renderCards()` のカード描画に表示を追加**

各カードの本文に以下を出す（`r.data.visitStatus`/`r.data.source`/`r.data.nominated`/`r.data.nameKana`/`r.data.menuItems`）:
```js
  const vs = r.data.visitStatus || 'reserved';
  const kana = r.data.nameKana ? `<span class="kana">（${r.data.nameKana}）</span>` : '';
  const menus = (r.data.menuItems && r.data.menuItems.length)
    ? r.data.menuItems.map(m => m.name).join('＆')
    : (r.data.menuName || '—');
  const badges = `<span class="vbadge v-${vs}">${VISIT_LABEL[vs] || vs}</span>` +
                 (r.data.source ? `<span class="sbadge">${SOURCE_LABEL[r.data.source] || ''}</span>` : '') +
                 (r.data.nominated ? `<span class="sbadge">⭐指名</span>` : '');
```
これらをカードのHTMLに差し込む（既存テンプレート文字列に `${kana}` `${menus}` `${badges}` を追加）。

CSS:
```css
.kana{color:#999;font-size:11px}
.vbadge{font-size:11px;padding:2px 8px;border-radius:10px;background:#eee}
.v-reserved{background:#e8eefc}.v-arrived{background:#e6f7ea}.v-unpaid{background:#fdeede}.v-paid{background:#eef0f2}.v-cancelled{background:#fde8e8}
.sbadge{font-size:11px;color:#777;margin-left:6px}
```

- [ ] **Step 3: 手動確認**

Run: `node server.js` → admin を開く
確認: 予約カードに 状態バッジ・経路・指名・カナ・複数メニュー（＆）が表示される。旧データ（`visitStatus` 無し）は「📅 予約」にフォールバックする。

- [ ] **Step 4: コミット**

```bash
git add admin.html
git commit -m "feat: admin予約一覧に来店状態/経路/指名/カナ/複数メニュー表示を追加"
```

---

## Task 8: 結合・後方互換・E2E確認

**Files:** （変更なし・検証のみ）

- [ ] **Step 1: 全単体テスト**

Run: `node --test test/`
Expected: 全 PASS

- [ ] **Step 2: 後方互換確認（旧ペイロード）**

Run:
```bash
node -e "require('dotenv').config(); const h=require('./api/reserve'); const res={_c:200,setHeader(){},status(c){this._c=c;return this},json(o){console.log(this._c,o);return this},end(){}}; h({method:'POST',headers:{},body:{date:'2026-07-09',time:'10:00',name:'旧式',phone:'09011112222',menuName:'リタッチ',menuId:'h1',staffCategory:'hair'}}, res);"
```
Expected: `200 { ok: true, id: ... }`（旧 menuId/staffCategory でも保存できる）

- [ ] **Step 3: SalonBoard送信が維持されていることを確認**

`reserve.js` が `status:'pending'` で積むこと、`worker.js` がそれを処理対象にすることを目視確認（worker.js の `processQueue` は `status==='pending'` を拾う）。実送信は本番運用時（IP冷却・セッション有効時）に worker 側で実施。

- [ ] **Step 4: BRIEF.md の「次のアクション」を更新**（ハンドオフ）

`.claude/projects/hpb-calendar/BRIEF.md` の次アクションに「予約管理内製化フェーズ1完了。会計(レジ)内製化が次サブプロジェクト」を反映。

- [ ] **Step 5: 最終コミット**

```bash
git add -A
git commit -m "chore: 予約管理内製化フェーズ1の結合確認・ハンドオフ更新"
```

---

## Self-Review チェック結果
- **Spec coverage:** メニューマスタ(§4.1)=T1-3 / 予約レコード拡張(§4.2)=T4-5 / 登録フォーム(§5.1)=T6 / プレート一覧(§5.2)=T7 / SalonBoard送信ON維持(§1,§7)=T5,T8 をカバー。
- **Placeholder:** TODO/TBD なし。各コードステップに実コードを記載。
- **型整合:** `menuItems[]` の要素 `{code,name,price,durationMin,service}` を T5/T6/T7 で統一。`getOccupiedSlotsForItems`/`serviceForItems`/`visitStatus`/`source` の名称が全タスクで一致。
- **未決(既定で進行):** 占有枠=合計所要時間（T4で実装）/ 顧客マスタ=sales-log流用（T6で `/api/customers` 利用）。
